[Unit]
Description=Flask App
After=network.target

[Service]
User=jenkins
WorkingDirectory=/var/lib/jenkins/workspace/flask-assignment-pipeline
ExecStart=/usr/bin/python3 app.py
Restart=always

[Install]
WantedBy=multi-user.target
